'use strict';

describe('sumcumoApp.viewShortProfile module', function() {

    beforeEach(module('sumcumoApp.viewShortProfile'));

    describe('viewShortProfile controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var viewShortProfileCtrl = $controller('ViewShortProfileCtrl');
            expect(viewShortProfileCtrl).toBeDefined();
        }));

    });
});