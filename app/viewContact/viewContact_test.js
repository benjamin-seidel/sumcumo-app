'use strict';

describe('sumcumoApp.viewContact module', function() {

    beforeEach(module('sumcumoApp.viewContact'));

    describe('viewContact controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var viewContactCtrl = $controller('ViewContactCtrl');
            expect(viewContactCtrl).toBeDefined();
        }));

    });
});