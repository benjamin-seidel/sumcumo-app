'use strict';

describe('sumcumoApp.viewTechInformation module', function() {

    beforeEach(module('sumcumoApp.viewTechInformation'));

    describe('viewTechInformation controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var viewTechInformationCtrl = $controller('ViewTechInformationCtrl');
            expect(viewTechInformationCtrl).toBeDefined();
        }));

    });
});